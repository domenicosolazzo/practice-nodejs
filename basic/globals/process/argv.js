// You can retrieve the console arguments using process.argv
// It returns an array:
// First element is node
// Second element is the file running in Node
// All the other elements are the console arguments
console.log(process.argv);